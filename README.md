This project create a function that return a readed line in a file descriptor. 
Used in a loop, reads a file line by line.

This project dive into static variables that persist its value between runs.
